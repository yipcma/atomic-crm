# Local development. The app talks to the API on the same origin (Vite proxies
# /api and /storage to localhost:3000, matching the Caddy reverse proxy in
# production), so there is no backend URL to configure here.
SKIP_PREFLIGHT_CHECK=true
